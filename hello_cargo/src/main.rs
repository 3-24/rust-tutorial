// 'cargo build  for compile & make executable
// 'cargo run'   for compile & make executable & run
// 'cargo check' for compile - for checking error
// 'cargo build --release' for compile it with optimizations
fn main() {
    println!("Hello, world!!");
}
